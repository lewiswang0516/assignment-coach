# Stage 03: Test Oracle

Before implementation help, state how you will know your code is correct.

## Oracles

<!-- Per task or method: expected input/output pairs, invariants, or the tests you wrote in coach-tests/. Provided assessed tests are referenced, never modified. -->

## Gate

This stage is complete for a task when it has at least one committed oracle: a written expected-behavior entry here or a runnable test under `coach-tests/`.
The Coach will not discuss implementation for a task without a committed oracle.
