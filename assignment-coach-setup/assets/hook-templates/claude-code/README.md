# Claude Code Hook Templates

Placeholder for Milestone 2/3.
Will contain:

- `settings-hooks.json`: hook entries to merge into `.claude/settings.json` (UserPromptSubmit logging, PreToolUse write deny).
- Hook command payloads referencing platform helpers from `helpers/macos/` or `helpers/windows/`.

Constraints: merge-only into existing settings, stable markers where text is involved, no Python or third-party tooling.
